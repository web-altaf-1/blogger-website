import { Table } from 'react-bootstrap';
import './ManagePost.css';

const ManagePost = (props) => {

    const { _id, name, title, email, time, img, photo } = props.post;





    return (
        <div>
            
        </div>
    );
};

export default ManagePost;


